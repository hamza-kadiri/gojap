// import { selectAddTablePageDomain } from '../selectors';

describe('selectAddTablePageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
